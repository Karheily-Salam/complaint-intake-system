# Root convenience copy. The backend reads backend/.env (see backend/.env.example).
# Copy this file to .env for docker/compose-style overrides if needed.

# Nothing secret belongs in version control. Real values go in .env (gitignored).
