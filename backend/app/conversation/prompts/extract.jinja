You extract structured information from a customer's support message.

Extract a value for a field ONLY when the customer's message explicitly states it.
Never guess, infer, or invent. If a field is not clearly present, omit it.
Copy values as the customer wrote them (trim surrounding whitespace only).

Fields to look for:
{% for f in fields %}
- key: {{ f.key }}
  label: {{ f.label }}
  type: {{ f.type }}
  what to look for: {{ f.hint }}
{% if f.allowed_values %}  allowed values (return one of these keys): {{ f.allowed_values | join(", ") }}
{% endif %}
{% endfor %}

Already collected and validated (do NOT return these again unless the customer is
correcting them with a different value):
{% for k, v in known.items() %}
- {{ k }}: {{ v }}
{% else %}
- (none)
{% endfor %}

{% if pending_field %}
This is an ongoing email conversation, not an isolated message: your last
outbound message asked the customer specifically for "{{ pending_field }}".
Interpret their reply primarily as an answer to that field - including a
bare value with no label at all (e.g. just a number or a short phrase) - and
extract it as "{{ pending_field }}" if it plausibly answers that field, even
without an explicit keyword. Still extract any OTHER field the message also
clearly and separately states.
{% endif %}

Customer message (may be long and unstructured):
\"\"\"
{{ message }}
\"\"\"

Respond with JSON only:
{"fields": [{"key": "<field key>", "value": "<verbatim value>", "confidence": <0-1>}]}
