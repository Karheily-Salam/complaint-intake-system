# Copy to frontend/.env.local for overrides. Vite only exposes VITE_-prefixed vars.

# Base URL for API calls. Leave as /api to use the dev proxy in vite.config.ts.
VITE_API_BASE_URL=/api/v1

# Where the dev proxy forwards /api requests.
VITE_API_PROXY_TARGET=http://localhost:8000
